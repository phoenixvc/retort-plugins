{
  "name": "@retort-plugins/router",
  "version": "0.2.0",
  "private": true,
  "description": "Keyword-based and semantic team/agent routing for Retort — answers natural-language questions about agent teams",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "optionalDependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
